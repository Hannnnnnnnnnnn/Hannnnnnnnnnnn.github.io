# ASSETS — 준비할 이미지

`MEDIA-MAP.md` 기준. 기존 25슬롯 → **정적 14장 + 프레임 1장 + 히어로 영상 2개**, 나머지는
데모가 대신한다.
남은 것은 전부 **잘라낸 구간**이라 세로로 긴 페이지를 통째로 찍을 일이 없다.

기계가 읽는 판본은 [`assets.json`](./assets.json).

파일이 들어오기 전까지 페이지에는 자리표시 상자와 경로가 그대로 보인다. 교체는 파일을
`images/` 에 떨어뜨리고 `<figure>` 안에 `<img>` 한 줄을 넣으면 끝난다. 프레임은
`<p class="frame__slot">` 을 `<img>` 로 갈아끼운다.

## 규칙 3줄

1. **히어로는 프레임 스냅샷 또는 타이포.** 전체 페이지를 크게 보여주려 하지 말 것
2. **Before/After 는 이제 한 군데뿐이다 — 01 Dec 02(길이 대비).** 03 Dec 02(CTA 라벨)는
   2026-08-31 소유자 판단으로 없앴다: **데모 C 가 같은 전환을 이미 조작 가능하게 보여준다.**
   Add to Cart ↔ Pre-order ↔ Sold out 을 토글로 바꿔 볼 수 있는데 그 옆에 같은 전환의 정지
   이미지를 두면 증거가 아니라 반복이다
3. **한 결정에 이미지 하나.** 두 장이 필요하면 결정이 두 개인 것이다 — 01 Dec 02 가 실제로
   그랬고, '줄이기'와 'Pairs Well With' 두 결정으로 갈렸다

---

## 프레임 (영상 4개)

| 파일 | 무엇 |
|---|---|
| `01-pdp/hero-mobile.mp4` | **들어옴.** 개편된 PDP 모바일 스크롤 녹화. 앞에 크게 |
| `01-pdp/hero-desktop.mp4` | **들어옴.** 같은 페이지 데스크톱 녹화. 모바일 뒤에 깔린다 |
| `02-plp/hero-mobile.mp4` | **들어옴.** 개편된 PLP 모바일 스크롤 녹화. 앞에 크게 |
| `02-plp/hero-desktop.mp4` | **들어옴.** 같은 페이지 데스크톱 녹화. 모바일 뒤에 깔린다. 원래 '데스크톱 없음'이던 자리 — 7 vs 11 세션이라 케이스와 모순이라는 판단이었으나 소유자가 넣기로 했다(2026-08-31) |

01·02 히어로는 정적 스냅샷 대신 화면 녹화가 들어갔다. 새 녹화를 넣을 때는 반드시 다시
인코딩할 것 — 원본은 120fps 라 모바일 한 개가 18.9MB 였고, 30fps·CRF 27~28 로 1.4MB 가
된다. 자세한 인코딩 값은 `assets.json` 의 `demo_assets` 마지막 항목.

## 01 · PDP Revamp (3)

| 파일 | 섹션 | 무엇 |
|---|---|---|
| `01-pdp/01.jpg` | 3 What I found | **들어옴.** Clarity 스크롤 히트맵 전체 페이지 2개(모바일+데스크톱). 원래 규칙은 '드롭오프 지점만 가로로 넓게'였는데 소유자가 전체본을 지정했다 |
| `01-pdp/02.jpg` | 4 Dec 01 | **들어옴(Before 만).** 옛 페이지의 리뷰 블록. After 는 아코디언 데모가 대신한다 |
| `01-pdp/03.jpg` | 4 Dec 02 | **들어옴.** 전체 길이 대비(Before/After, 각각 모바일+데스크톱) |

Dec 03 은 **Pairs Well With 레일**(가로 스크롤, 라이브 메타필드의 페어링 9개), Dec 04 는
**데모 A**, Dec 05 는 **데모 B**(상품 정보 칼럼)가 자리를 가져갔다. `MEDIA-MAP.md` 는
Dec 03 을 정적 1장, Dec 05 를 정적 2장으로 두라고 하지만 셋 다 인터랙티브로 남아 있다 —
소유자 요청이고, 되돌릴 때의 기준은 `CLAUDE.md` 데모 표에 적혀 있다.

## 02 · PLP Revamp (5)

| 파일 | 섹션 | 무엇 |
|---|---|---|
| ~~`02-plp/01.jpg`~~ | 2 The problem | **불필요.** HTML `.dist` 막대 차트가 대체 |
| `02-plp/twist-bag-*-2.jpg` (6장) | 4 Dec 01 | **들어옴.** 데모 F 의 호버 사진 — 색마다 그 바리언트의 두 번째 컷. `#color_<handle>` 그룹의 2번째 미디어를 480x600 으로 받았다(13~20KB). 첫 컷은 `01-pdp/twist-bag-*.jpg` 재사용 |
| ~~`02-plp/02.jpg`~~ | 4 Dec 01 | **불필요.** 데모 F(현재 카드 Try it)가 대체 — before 는 보여주지 않기로 했다(2026-08-31) |
| ~~`02-plp/03.jpg`~~ | 4 Dec 02 | **불필요.** HTML `.mfd` 도식이 대체 |
| `02-plp/04-cross-sell.jpg` | 4 Dec 04 | **들어옴.** 백 컬렉션용 크로스셀 배너(Miffy studio mixed) |
| `02-plp/04-promotion.jpg` | 4 Dec 04 | **들어옴.** 광고 랜딩용 프로모션 배너(Nami). 표의 'Promotion, or best sellers' 중 프로모션 쪽 |
| `02-plp/05.jpg` | 6 Outcome | **들어옴.** Clarity 신·구 스크롤 히트맵. 소유자 `Frame 18.png` 652x4252 RGBA → 흰 배경 합성 후 JPEG 222KB(원본 1.57MB). 한 장 안에 신·구 두 컬럼이 이미 나란히 들어 있다 |

Dec 03 은 **데모 A**. 히트맵이 히어로가 아니라 결과 섹션에 있는 이유는 스크롤이 이
테스트의 결과이기 때문이다. 2026-08-31 소유자 지시로 히트맵을 섹션 맨 위에서 내려 **스크롤 착시
데모 옆**에 `.cmp` 두 칸으로 나란히 놓았다 — 재는 것(히트맵)과 읽는 법(데모)이 한 화면에 있어야
"같은 수치, 다른 결론"이 성립한다. 아주 세로로 긴 이미지라 `.cmp__window`(absolute 스크롤러)가
행 높이를 먹지 않는 게 핵심이다.

## 03 · Pre-order Experience (0) — 촬영할 것이 없다

| 파일 | 섹션 | 무엇 |
|---|---|---|
| ~~`03-preorder/01.jpg`~~ | 2 The problem | **불필요.** HTML `.dist--months` 차트가 대체 — 이 케이스의 첫 근거라 320px 에서 읽혀야 한다 |
| ~~`03-preorder/02.jpg`~~ | 4 Dec 02 | **불필요.** 데모 C 가 같은 CTA 전환을 조작 가능하게 이미 보여준다 (규칙 2 참조) |
| ~~`03-preorder/03.jpg`~~ | 4 Dec 03 | **불필요.** 내용이 100% 텍스트고 같은 문장이 이미 페이지에 세 번 있다 — HTML `.anno` 가 대체 |
| ~~`03-preorder/04-email.jpg`~~ | 4 Dec 04 | **불필요.** 이메일이 존재한다는 것은 `.mfd--chain` 의 Klaviyo 칸이 말한다. 내용은 본문이 인용하고 있어 스크린샷이 더할 것이 없다 |

**이 케이스는 래스터 이미지가 0장이다.** 히어로가 타이포 전용이고 나머지가 전부 데모 C +
HTML 컴포넌트 3개라, work-3 에는 사진이 한 장도 들어가지 않는다. 의도된 상태이지 미완이
아니다 — 케이스의 논지 자체가 "무엇이 공간을 차지할 자격이 있는가"다. **되돌린다면 기준은
하나: 어떤 자리의 논지가 "이렇게 동작한다"가 아니라 "실제로 이렇게 생겼다"로 바뀔 때만.**
그때는 `index.html` 03 행의 `data-preview` 도 같이 살려야 한다(아래).

`index.html` 의 03 행은 `data-preview` 가 **없다.** 01·02 행은 히어로 영상을 미리보기로
쓰지만 03 은 보여줄 화면이 없어서(히어로가 타이포 전용인 것과 같은 이유) 속성을 지웠다.
`main.js` 는 `.row[data-preview]` 만 고르므로 그 행은 조용히 미리보기가 없을 뿐이다.

트리거 체인은 상자-라벨 도식이라 **사진이 아니라 HTML `.mfd--chain`** 이다.

히어로는 이미지가 없다 — 문구 자체를 크게 쓴 타이포. Dec 01 은 **데모 C**.

---

## 넣지 말 것

- 표와 산문이 증거인 섹션(01 §2·§6, 02 §3·§5, 03 §3·§5·§6)에 새 이미지를 만들어 넣지 말 것.
  **빈 것이 의도된 상태**다
- 스톡 이미지, 일러스트, AI 생성 비주얼
- 미피가 보이는 이미지는 데모 안에 넣지 말 것 (라이선스 IP — 데모의 상품 자리는 회색 블록)
